version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "legend-simple-app"
        LoadBalancerInfo:
          ContainerName: "app-container"
          ContainerPort: 80